{% extends 'base.html.twig' %}

{% block title %}Members{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('pm_home') }}">Home</a></li>
        <li class="breadcrumb-item"><a href="{{ path('pm_work_members') }}">Members</a></li>
        <li class="breadcrumb-item"><a href="{{ path('pm_work_member', {'id': member.id}) }}">{{ member.name }}</a></li>
        <li class="breadcrumb-item active">Move</li>
    </ol>
{% endblock %}

{% block body %}

    {{ include('works/members/_tabs.html.twig') }}

    <div class="card">
        <div class="card-body">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Move</button>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}
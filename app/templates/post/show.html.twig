{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post
        <a class="h6" href="{{ path('pm_posts') }}">back to list</a>
        <span class="h5">or</span>
        <a class="h6" href="{{ path('pm_post_edit', {'id': post.id}) }}">edit</a>
    </h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ post.text }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('post/_delete_form.html.twig') }}
{% endblock %}
